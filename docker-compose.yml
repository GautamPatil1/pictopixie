version: '3'

services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "6000:3000"
    depends_on:
      - backend
    environment:
      - WDS_SOCKET_PORT=0


  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "4000:8000"
    